DATABASE_URL="file:./dev.db"
PORT=4000
HOST=0.0.0.0
UPLOADS_DIR="./uploads"
# Where the API serves the built web frontend from in production.
# Defaults to ../web/dist relative to apps/api when not set.
# STATIC_DIR="../web/dist"
